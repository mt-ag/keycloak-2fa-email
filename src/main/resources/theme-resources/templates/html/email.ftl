<div style="font-family: Helvetica, Verdana, sans-serif;">
    <p>${msg("email.2fa.mail.salutation")}</p>

    <p>${msg("email.2fa.mail.prompt")}</p>

    <p style="margin: 1em 0; font-size: 1.6em; font-weight: 700;">${code}</p>

    <p>${msg("email.2fa.mail.ttl", ttl)}</p>

    <p>${msg("email.2fa.mail.safety.info")}</p>
</div>
